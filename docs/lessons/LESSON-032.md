# LESSON-032@v1

```yaml
id: LESSON-032@v1
claim: "A frozen research layer is reopened only when a downstream decision needs evidence that does not yet exist — never on curiosity; and a freeze constrains the interface contract, not future improvement of what sits behind it."
type: GOVERNANCE
evidence:
  - "PROGRAM_BOARD V6 (2026-06-30): 'The Evidence Layer (Object · Operations · Set · Snapshot) is
    declared FROZEN as stable architecture — no new Object/Operation/Set/Snapshot unless data shows a
    large logic gap' ('if we keep changing the foundation, Decision Science never begins')"
  - "PROGRAM_BOARD V8 P90 (APPROVED): 'a frozen layer guarantees interface stability, not implementation
    immutability' — performance/storage/serialization may improve; the contract does not move"
  - "PROGRAM_BOARD V11 alpha philosophy (2026-07-03): 'we return to Market Science ONLY when the Decision
    Layer says: I need new evidence that does not exist — not before; the freeze is genuine.' F-005 re-run
    = debt (deferred); Master Architecture: P62 → reopenable-by-knowledge-need (two-tier)"
counter_evidence: "none found (scope: Market/Decision doctrine governance). Bound: a freeze is NOT
  implementation immutability (P90 — the code behind the contract keeps improving) and NOT permanent — it
  is designed to reopen, but only on a proven downstream need, not on a hunch that 'more research helps'"
validity_conditions: general (any layered system where a foundation must stabilize before higher layers
  can be built; demonstrated on the Evidence Layer freeze and the Market-Science freeze)
when_to_use: deciding whether to reopen settled work — reopen only when a concrete downstream consumer
  cannot proceed without evidence that provably does not yet exist; otherwise treat the freeze as a real
  constraint and build on the interface, improving the implementation behind it as needed
when_not_to_use: do not invoke 'freeze' to block a genuine downstream need (that is the trigger to reopen),
  and do not read 'frozen' as 'the implementation can never change' — P90 separates the stable contract
  from the mutable internals; the freeze governs the interface, not the engine room
provenance: {principle: P90/P62, doctrine: (V6 Evidence Layer FROZEN / V8 / V11 alpha philosophy), finding: F-005}
lifecycle: ACTIVE  # Chief review 2026-07-05
```

**Maelezo kwa mwanafunzi:** "freeze" haimaanishi code haitabadilika — P90 inatenga *contract* (imesimama) na *implementation*
(inaboreshwa). Wala si ya kudumu: inafunguliwa TU pale layer ya juu inaposema "nahitaji evidence ambayo
haipo" (V11). Kurudi Market Science kwa udadisi tu = kuvunja msingi kabla Decision Science haijaanza.
