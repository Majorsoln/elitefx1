# LESSON-027@v1

```yaml
id: LESSON-027@v1
claim: "The atomic unit of a tradable claim is the full configuration (event × market context), not the event; evaluate and attribute at the configuration level, because no event carries universal edge."
type: METHOD
evidence:
  - "PROGRAM_BOARD F-020 (APPROVED): 'Trading edge emerges from Event × Configuration'; Q-019 CLOSED —
    Configuration confirmed the atomic unit (Phase 6/6.5)"
  - "PROGRAM_BOARD F-021 (APPROVED): 'No event possesses universal edge' — the same event is +EV in one
    configuration and −EV in another (event_reality_report.md Phase 12: mean_reversion EURUSD +0.90 vs
    aggregate −0.212)"
counter_evidence: "the configuration is the unit of ANALYSIS, not a guarantee of edge — no configuration
  survived pre-registered OOS + FDR (Phase 14: 282 → 0). And configurations keyed on raw coordinate
  cells do NOT generalize cross-pair (LESSON-005/F-014). 'Atomic unit' names where to look, not where edge is"
validity_conditions: general as method (unit-of-analysis principle); the market grounding is FX
  (9 pairs, 2016-2024, event × vol/context configurations)
when_to_use: designing any edge search, attribution, or rejection — test at the configuration level
  (event conditioned on its context), never treat an event label as a self-contained signal; a result
  'for event X' with the configuration pooled away is not interpretable
when_not_to_use: do not read configuration-level EV as tradable (in-sample; 0/282 OOS); do not build a
  rule engine on raw coordinate configurations (they fail cross-pair — LESSON-005); the principle is
  about the unit of measurement, not permission to trade a configuration
provenance: {finding: F-020/F-021, phase: 6/6.5, doctrine: (Permanent Truth 6 — Configuration = atomic unit)}
lifecycle: ACTIVE  # Chief review 2026-07-05
```

**Maelezo kwa mwanafunzi:** event ileile (mean_reversion) ni +0.90 EURUSD lakini −0.212 kwa jumla —
"edge ya event" bila context haina maana. Kitengo cha msingi cha kupima/kukataa ni **Configuration**
(event × context), si event. Lakini hii ni RAMANI ya wapi kupima, si uthibitisho wa edge (0/282 OOS).
